namespace Diagrams
{
    /// <summary>Represents point|shape relationship types.</summary>
    /// <exclude />
    public enum Containment
    {
        none,
        inside,
        coincident,
        outside,
    }
}